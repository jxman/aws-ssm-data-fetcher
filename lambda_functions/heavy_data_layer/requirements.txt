# Heavy Data Processing Layer
# Large dependencies for data processing and report generation
# Only used by processor and excel-generator functions

# Data processing dependencies
pandas>=2.0.0
numpy>=1.24.0

# Excel generation
openpyxl>=3.1.0
et-xmlfile>=1.1.0

# Note: This layer may be 40-45MB but only used by functions that need it
# Note: Core dependencies (requests, feedparser, etc.) are in core_layer
