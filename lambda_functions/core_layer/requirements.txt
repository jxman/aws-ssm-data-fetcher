# Core Layer Dependencies
# Essential lightweight dependencies used by all Lambda functions
# This layer should stay under 10MB to ensure fast loading

# HTTP requests (required by RSS client and error handling)
requests>=2.31.0
certifi>=2023.7.22
urllib3>=2.0.0

# RSS parsing (required by RSS client)
feedparser>=6.0.10

# Date/time parsing (required by all functions)
python-dateutil>=2.8.2

# Logging enhancements (shared by all)
structlog>=23.2.0

# Note: boto3/botocore available in Lambda runtime
# Note: This layer contains NO heavy data processing dependencies
